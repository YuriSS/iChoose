const { K } = require("../../helpers")

const selectors = {}

selectors.idKey = K("id"),

selectors.totalKey = K("total")

selectors.votesKey = K("votes")

selectors.restaurantIdKey = K("restaurantId")

module.exports = selectors
